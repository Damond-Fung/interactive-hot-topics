# 示例请求

## 直接出报告

- 帮我生成上周 `互动交流` 板块的热门讨论报告，导出 Excel
- 帮我看上周 `互动交流` 大家讨论最热的 5 个主题是什么，再把高频负向反馈单独列出来
- 帮我统计 `互动交流` 本周最集中的讨论内容是什么，顺便看下产品建议最多的是哪些方向
- 帮我做一份 `互动交流` 周报，重点看热门主题、负向反馈和产品建议

## 指定时间范围

- 帮我统计 `互动交流` 在 `2026-05-01` 到 `2026-05-31` 的热门话题、负向反馈和产品建议
- 帮我看 `5 月第一周` `互动交流` 里讨论热度最高的内容是什么
- 帮我整理 `4 月整月` `互动交流` 的热门讨论，并附上全量明细页签
- 帮我按北京时间统计 `互动交流` 最近 7 天的话题明细和汇总

## 先导底表

- 帮我先导出上周 `互动交流` 的全量帖子明细，不要直接给我最终 TOP5
- 先把 `互动交流` 上周所有话题都拉下来，给我 `汇总 + 明细`，我先核对采集结果
- 帮我导出 `analysis_packets` 和 `ai-template`，我确认判别口径后再回填正式结果
- 先给我 `互动交流` 的底表和 `ai-review`，最终结论等我确认后再生成

## 正式回填

- 这是正式 `ai_results.json`，帮我回填并生成最终版 `互动交流热门讨论报告`
- 我已经补完逐帖 AI 判别结果了，你帮我重新汇总 `热门话题主题 TOP5`、`高频负向反馈 TOP5` 和 `产品建议 TOP5`
- 不要再给我待判别状态了，直接基于这个 `ai_results` 生成最终版报告
- 帮我把这份 `ai_results.json` 回填进上周 `互动交流` 底表，导出正式 Excel 和 JSON
